@{
    ViewBag.Title = "Checkout";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@using Capstone.Models;


<div class="content">
    <div class="content_top">
        <div class="heading">
            <h3>Checkout</h3>
        </div>
        <div class="clear"></div>
    </div>
    <div class="main">
        @using (Html.BeginForm("index", "Checkout", FormMethod.Post, new { id = "loginForm" }))
        {
        <div class="content">
            <div class="billing_address">
                <h3>Billing Address</h3>
                <p>Sign in with the form below.</p>
               
                    <input name="firstName" type="text" value="firstName" class="field" onfocus="this.value = '';" onblur="if (this.value == '') {this.value = 'First Name';}">
                    <input name="lastName" type="text" value="lastName" class="field" onfocus="this.value = '';" onblur="if (this.value == '') {this.value = 'Last Name';}">
                    <input name="email" type="text" value="email" class="field" onfocus="this.value = '';" onblur="if (this.value == '') {this.value = 'Email';}">
                    <input name="phone" type="text" value="phone" class="field" onfocus="this.value = '';" onblur="if (this.value == '') {this.value = 'Phone';}">
                    <input name="address" type="text" value="address" class="field" onfocus="this.value = '';" onblur="if (this.value == '') {this.value = 'Address';}">
                    <input name="city" type="text" value="city" class="field" onfocus="this.value = '';" onblur="if (this.value == '') {this.value = 'City';}">
                    <select  name="state" class="form-control" required>
                        <option value="" selected="selected">- Select A State -</option>
                        @foreach (var states in ViewBag.StatesList)
                        {
                            <option value="@states.StateID">@states.StateName</option>
                        }
                    </select>
                
            </div>
            <div class="shipping_method">
                <h3>Shipping Method</h3>
                <p>Please Select A Shipping Method</p>
               
                    @foreach (ShippingMethod shipMethod in ViewBag.listShippingMethod)
                    {   
                        <table>
                           <p> @shipMethod.type
                            <br />
                            @Html.RadioButtonFor(m => shipMethod.shippingMethod_ID, shipMethod.type)
                            @shipMethod.shippingCost
                            </p>
                        </table>
                    <br />                                      
                    }          
                
            </div>
            <div class="payment_method">
                <h3>Payment Method</h3>
                <p>Sign in with the form below.</p>
             
                    <input name="email" type="text" value="Username" class="field" onfocus="this.value = '';" onblur="if (this.value == '') {this.value = 'Username';}">
                    <input name="password" type="password" value="Password" class="field" onfocus="this.value = '';" onblur="if (this.value == '') {this.value = 'Password';}">
                
            </div>
            <div class="clear"></div>
            <div class="review_order">
                <h3>Review Order</h3>
                <p>Sign in with the form below.</p>
               
                @{ decimal checkoutTotal = 0; }
                <div id="Checkout">
                    @if (Session["cart"] != null)
                    {
                        <table>
                            <tr>
                                <th>Option</th>
                                <th>Name</th>
                                <th>Price</th>
                                <th>QTY</th>
                                <th>Sub Total</th>
                                <th>Total</th>
                            </tr>

                            @foreach (Item item in (List<Item>)Session["cart"])
                            {
                                <tr>
                                    <td>@Html.ActionLink("Delete", "Delete", "Checkout", new { product_id = item.Pr.product_ID, category_id = item.Pr.category_ID }, null)</td>
                                    <td>@item.Pr.name</td>
                                    <td>@item.Pr.price</td>
                                    <td>@item.Quantity</td>
                                    <td>@(item.Pr.price * item.Quantity)</td>
                                    @{checkoutTotal += (item.Pr.price * item.Quantity);}
                                </tr>
                            }
                            <tr>
                                <td></td>
                                <td></td>
                                <td></td>
                                <td></td>
                                <td></td>
                                <td id="total">Total = $@checkoutTotal</td>                            
                            </tr>
                        </table>
                    }
                </div>
            </div>
            <div class="clear"></div>
        </div>
        <div class="checkout">
            <h4><a href="~/Home">Checkout</a></h4>
        </div>
         }
    </div>
</div>